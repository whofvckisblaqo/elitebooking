export const metadata = {
  title: "Contact Us",
  description: "Get in touch with the EliteBooking team. We are here to help with your celebrity booking needs.",
};

export default function ContactLayout({ children }) {
  return children;
}